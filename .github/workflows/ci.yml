name: Face Editor CI

on:
  push:
    branches: [main, chatgpt/phase1-foundation]
  pull_request:
    branches: [main]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
      - run: npm install
      - run: npm test
      - run: npm run build
