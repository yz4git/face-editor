# Face Editor

Three.js-based 2D polygon character editor. The project treats every visible character part as flat triangle mesh data so an edited character can be reused directly in a game without baking the result to a texture.

Implementation is being developed from the repository architecture documented in `docs/ARCHITECTURE.md`.
